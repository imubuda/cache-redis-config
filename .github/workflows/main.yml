name: cache-redis-config

on:
  push:
    branches:
      - main
      - feature/*:
        branches:
          - main
  pull_request:
    branches:
      - main
      - feature/*:
        branches:
          - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run echo command
        run: |
          echo "Cache Redis configuration is ready"